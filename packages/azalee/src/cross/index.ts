/**
 * Inazuma Eleven Cross (jeu mobile Unity/Addressables, distinct de Victory
 * Road) : schéma masterdata, enums, catalogue d'assets et statut d'extraction.
 *
 * ⚠ Module **serveur** (lecture des artefacts). Les types et helpers purs
 * vivent dans `./data-shared` et `./assets-shared`.
 */

export * from "./data";
